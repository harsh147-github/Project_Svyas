/**
 * Legacy barrel — the n8n-aligned implementation lives in `n8n-workflow.ts`.
 * `daily-pipeline` Inngest function calls those steps directly.
 */

export type { PhaseResult } from '@/lib/pipeline/n8n-workflow'
